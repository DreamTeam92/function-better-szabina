#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../eslint-config-prettier/bin/cli.js" "$@"
else
  exec node  "$basedir/../../../eslint-config-prettier/bin/cli.js" "$@"
fi
# cmd-shim-target=/home/nyulik/function-better-szabina/app/frontend/node_modules/.pnpm/eslint-plugin-prettier@5.5.6_eslint-config-prettier@10.1.8_eslint@9.39.5_jiti@1.21.7____a13dec699db6ecafaadea55c779a73e9/node_modules/eslint-config-prettier/bin/cli.js
